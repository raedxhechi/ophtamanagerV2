import { NotImplemented } from "@/components/not-implemented";

export const metadata = { title: "Search" };

export default function AdminSearchPage() {
  return (
    <NotImplemented
      title="Search"
      description="Search across patients, orders and policies will live here. Each of those lists already has its own search box in the meantime."
    />
  );
}
